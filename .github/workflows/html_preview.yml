name: HTML Preview

on:
  workflow_dispatch:

jobs:
  preview:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Confirm input file exists
        run: |
          ls -la
          test -f market_data.json

      - name: Generate preview HTML
        run: |
          python html_generator.py market_data.json report.html

      - name: Upload preview
        uses: actions/upload-artifact@v4
        with:
          name: html-preview
          path: report.html
